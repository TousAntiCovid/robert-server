public class CleaClientStepDefinitions {
    public CleaClientStepDefinitions() {
        Given("^\"([^\"]*)\" registered on TAC$", (String arg0) -> {
            // TODO Robert registration of the user
        });
    }
}
